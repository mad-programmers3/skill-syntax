export const getters = {
    dataList(state) {
        return state.dataList;
    },
    formData(state) {
        return state.formData;
    },
    Config(state) {
        return state.Config;
    },
};