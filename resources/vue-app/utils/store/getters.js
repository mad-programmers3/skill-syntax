export const getters = {
    dataList(state) {
        return state.dataList;
    },
    formData(state) {
        return state.formData;
    },
    Config(state) {
        return state.Config;
    },
    showHeader(state) {
        return state.showHeader;
    }
};